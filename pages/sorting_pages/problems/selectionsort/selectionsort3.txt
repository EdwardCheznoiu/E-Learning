int n, X[100];
for(int i = 0 ; i < n - 1 ; i ++)
    for(int j = i + 1 ; j < n ; j ++)
        if(X[i] > X[j] && x[i] % 2 =! 0)
        {
            int aux = X[i];
            X[i] = X[j];
            X[j] = aux;
        }